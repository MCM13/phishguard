# URL pública del backend (build de producción).
# Este fichero NO contiene secretos; sólo la URL pública del API.
# Es seguro versionarlo en git.
# Sustituye por la URL de tu backend o configúrala en Cloudflare Pages / Render (build env).
VITE_API_URL=https://tu-servicio-backend.onrender.com
